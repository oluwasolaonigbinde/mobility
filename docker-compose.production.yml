name: cardvert-release

x-backend-environment: &backend-environment
  APP_NAME: cardvert-api
  ENVIRONMENT: production
  RELEASE_REVISION: ${RELEASE_REVISION:?RELEASE_REVISION is required}
  API_V1_PREFIX: /api/v1
  DATABASE_URL: ${DATABASE_URL:?DATABASE_URL is required}
  REDIS_URL: ${REDIS_URL:?REDIS_URL is required}
  PGSSLROOTCERT: /run/secrets/postgres_tls_ca
  SENTRY_DSN: ${SENTRY_DSN:-}
  BACKEND_CORS_ORIGINS: ${BACKEND_CORS_ORIGINS:?BACKEND_CORS_ORIGINS is required}
  LOG_LEVEL: ${LOG_LEVEL:-INFO}
  LOG_FORMAT: json
  REQUEST_ID_HEADER: X-Request-ID
  JWT_SECRET_KEY: ${JWT_SECRET_KEY:?JWT_SECRET_KEY is required}
  JWT_ALGORITHM: HS256
  PAYOUT_CRYPTO_KEYRING_B64: ${PAYOUT_CRYPTO_KEYRING_B64:?PAYOUT_CRYPTO_KEYRING_B64 is required}
  PAYOUT_CRYPTO_KEY_VERSION: ${PAYOUT_CRYPTO_KEY_VERSION:?PAYOUT_CRYPTO_KEY_VERSION is required}
  INVOICE_ISSUER_EXTERNAL_INPUT_REFERENCE: ${INVOICE_ISSUER_EXTERNAL_INPUT_REFERENCE:-}
  ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES:-60}
  SESSION_ABSOLUTE_LIFETIME_MINUTES: ${SESSION_ABSOLUTE_LIFETIME_MINUTES:-720}
  LOGIN_RATE_LIMIT_IP_MAX_FAILURES: ${LOGIN_RATE_LIMIT_IP_MAX_FAILURES:-150}
  LOGIN_RATE_LIMIT_IP_WINDOW_SECONDS: ${LOGIN_RATE_LIMIT_IP_WINDOW_SECONDS:-300}
  LOGIN_RATE_LIMIT_ACCOUNT_MAX_FAILURES: ${LOGIN_RATE_LIMIT_ACCOUNT_MAX_FAILURES:-5}
  LOGIN_RATE_LIMIT_ACCOUNT_WINDOW_SECONDS: ${LOGIN_RATE_LIMIT_ACCOUNT_WINDOW_SECONDS:-900}
  LOGIN_RATE_LIMIT_GLOBAL_MAX_FAILURES: ${LOGIN_RATE_LIMIT_GLOBAL_MAX_FAILURES:-250}
  LOGIN_RATE_LIMIT_GLOBAL_WINDOW_SECONDS: ${LOGIN_RATE_LIMIT_GLOBAL_WINDOW_SECONDS:-300}
  ALLOW_DEMO_SEED: "false"
  PRIVACY_DISCLOSURE_SYNTHETIC_TEST_MODE: "false"
  LOGIN_RATE_LIMIT_TRUST_CLIENT_IP_HEADER: ${LOGIN_RATE_LIMIT_TRUST_CLIENT_IP_HEADER:-true}
  LOGIN_RATE_LIMIT_TRUSTED_PROXY_CIDRS: ${LOGIN_RATE_LIMIT_TRUSTED_PROXY_CIDRS:-10.255.254.10/32}
  DRIVER_REGISTRATION_ENABLED: ${DRIVER_REGISTRATION_ENABLED:-false}
  DRIVER_REGISTRATION_RATE_LIMIT_IP_MAX_ATTEMPTS: ${DRIVER_REGISTRATION_RATE_LIMIT_IP_MAX_ATTEMPTS:-10}
  DRIVER_REGISTRATION_RATE_LIMIT_IP_WINDOW_SECONDS: ${DRIVER_REGISTRATION_RATE_LIMIT_IP_WINDOW_SECONDS:-3600}
  DRIVER_REGISTRATION_RATE_LIMIT_EMAIL_MAX_ATTEMPTS: ${DRIVER_REGISTRATION_RATE_LIMIT_EMAIL_MAX_ATTEMPTS:-3}
  DRIVER_REGISTRATION_RATE_LIMIT_EMAIL_WINDOW_SECONDS: ${DRIVER_REGISTRATION_RATE_LIMIT_EMAIL_WINDOW_SECONDS:-3600}
  DRIVER_REGISTRATION_RATE_LIMIT_TRUST_CLIENT_IP_HEADER: ${DRIVER_REGISTRATION_RATE_LIMIT_TRUST_CLIENT_IP_HEADER:-false}
  DRIVER_REGISTRATION_RATE_LIMIT_TRUSTED_PROXY_CIDRS: ${DRIVER_REGISTRATION_RATE_LIMIT_TRUSTED_PROXY_CIDRS:-}
  F7_SEED_MAX_TRIPS_PER_DAY: ${F7_SEED_MAX_TRIPS_PER_DAY:-2}
  PASSWORD_MIN_LENGTH: ${PASSWORD_MIN_LENGTH:-12}
  DEFAULT_CURRENCY: ${DEFAULT_CURRENCY:-NGN}
  MAX_CAMPAIGN_ZONE_AREA_SQ_KM: ${MAX_CAMPAIGN_ZONE_AREA_SQ_KM:-5000}
  MAX_LOCATION_PINGS_PER_BATCH: ${MAX_LOCATION_PINGS_PER_BATCH:-500}
  LOCATION_PING_FUTURE_SKEW_SECONDS: ${LOCATION_PING_FUTURE_SKEW_SECONDS:-300}
  LOCATION_PING_START_SKEW_SECONDS: ${LOCATION_PING_START_SKEW_SECONDS:-900}
  MAX_LOCATION_ACCURACY_M: ${MAX_LOCATION_ACCURACY_M:-10000}
  MAX_LOCATION_SPEED_MPS: ${MAX_LOCATION_SPEED_MPS:-120}
  WORKER_SWEEP_INTERVAL_MINUTES: ${WORKER_SWEEP_INTERVAL_MINUTES:-5}
  WORKER_SWEEP_BATCH_SIZE: ${WORKER_SWEEP_BATCH_SIZE:-25}
  FRAUD_REVIEW_SLA_DAYS: ${FRAUD_REVIEW_SLA_DAYS:-7}
  FRAUD_ASSESSMENT_FORMULA_VERSION: ${FRAUD_ASSESSMENT_FORMULA_VERSION:-fraud_assessment_v1}
  ROUTE_REPLAY_DETECTOR_VERSION: ${ROUTE_REPLAY_DETECTOR_VERSION:-route_replay_v1}
  ROUTE_REPLAY_COORDINATE_PRECISION: ${ROUTE_REPLAY_COORDINATE_PRECISION:-5}
  ROUTE_REPLAY_TIME_TOLERANCE_SECONDS: ${ROUTE_REPLAY_TIME_TOLERANCE_SECONDS:-5}
  ROUTE_REPLAY_MIN_VALID_PINGS: ${ROUTE_REPLAY_MIN_VALID_PINGS:-10}
  ROUTE_REPLAY_MIN_DISTANCE_M: ${ROUTE_REPLAY_MIN_DISTANCE_M:-250}
  ROUTE_REPLAY_MAX_EVIDENCE_MATCHES: ${ROUTE_REPLAY_MAX_EVIDENCE_MATCHES:-10}
  PING_RETENTION_MONTHS: ${PING_RETENTION_MONTHS:-12}
  PARTITION_PREMAKE_MONTHS: ${PARTITION_PREMAKE_MONTHS:-4}
  PRIVACY_DISCLOSURE_LIVE_AUTHORIZED: ${PRIVACY_DISCLOSURE_LIVE_AUTHORIZED:-false}
  MEASUREMENT_LIVE_ISSUANCE_AUTHORIZED: ${MEASUREMENT_LIVE_ISSUANCE_AUTHORIZED:-false}
  MEASUREMENT_REPORT_METHOD_REFERENCE: ${MEASUREMENT_REPORT_METHOD_REFERENCE:-}
  MEASUREMENT_ROI_METHOD_REFERENCE: ${MEASUREMENT_ROI_METHOD_REFERENCE:-}
  PRIVACY_LEGAL_APPROVAL_REFERENCE: ${PRIVACY_LEGAL_APPROVAL_REFERENCE:-}
  PRIVACY_DISCLOSURE_CONFIG_REFERENCE: ${PRIVACY_DISCLOSURE_CONFIG_REFERENCE:-}
  PRIVACY_QUERY_HISTORY_RETENTION_REFERENCE: ${PRIVACY_QUERY_HISTORY_RETENTION_REFERENCE:-}
  PRIVACY_QUERY_HISTORY_RETENTION_DAYS: ${PRIVACY_QUERY_HISTORY_RETENTION_DAYS:-30}
  PRIVACY_MIN_VEHICLES_PER_CELL: ${PRIVACY_MIN_VEHICLES_PER_CELL:-3}
  PRIVACY_MIN_TRIPS_PER_CELL: ${PRIVACY_MIN_TRIPS_PER_CELL:-5}
  PRIVACY_MIN_DAYS_PER_CELL: ${PRIVACY_MIN_DAYS_PER_CELL:-2}
  PRIVACY_MAX_CONTRIBUTOR_SHARE: ${PRIVACY_MAX_CONTRIBUTOR_SHARE:-0.5}
  PRIVACY_MIN_RESOLUTION_M: ${PRIVACY_MIN_RESOLUTION_M:-500}
  DSR_APPROVED_EXCEPTION_REFERENCES: ${DSR_APPROVED_EXCEPTION_REFERENCES:-}
  OBJECT_STORAGE_ENDPOINT_URL: ${OBJECT_STORAGE_ENDPOINT_URL:?OBJECT_STORAGE_ENDPOINT_URL is required}
  OBJECT_STORAGE_PUBLIC_ENDPOINT_URL: ${OBJECT_STORAGE_PUBLIC_ENDPOINT_URL:?OBJECT_STORAGE_PUBLIC_ENDPOINT_URL is required}
  OBJECT_STORAGE_REGION: ${OBJECT_STORAGE_REGION:?OBJECT_STORAGE_REGION is required}
  OBJECT_STORAGE_BUCKET: ${OBJECT_STORAGE_BUCKET:?OBJECT_STORAGE_BUCKET is required}
  OBJECT_STORAGE_ACCESS_KEY_ID: ${OBJECT_STORAGE_ACCESS_KEY_ID:?OBJECT_STORAGE_ACCESS_KEY_ID is required}
  OBJECT_STORAGE_SECRET_ACCESS_KEY: ${OBJECT_STORAGE_SECRET_ACCESS_KEY:?OBJECT_STORAGE_SECRET_ACCESS_KEY is required}
  MALWARE_SCANNER_HOST: ${MALWARE_SCANNER_HOST:-}
  MALWARE_SCANNER_PORT: ${MALWARE_SCANNER_PORT:-3310}
  FILE_KYC_RETENTION_DAYS: ${FILE_KYC_RETENTION_DAYS:-}
  INSTALLATION_EVIDENCE_UPLOADER_ROLES: ${INSTALLATION_EVIDENCE_UPLOADER_ROLES:-}
  INSTALLATION_EVIDENCE_REQUIRED_VIEWS: ${INSTALLATION_EVIDENCE_REQUIRED_VIEWS:-}
  INSTALLATION_EVIDENCE_VALIDITY_HOURS: ${INSTALLATION_EVIDENCE_VALIDITY_HOURS:-}
  DISPLAY_PROOF_CHALLENGE_TTL_SECONDS: ${DISPLAY_PROOF_CHALLENGE_TTL_SECONDS:-}
  DISPLAY_PROOF_VALIDITY_SECONDS: ${DISPLAY_PROOF_VALIDITY_SECONDS:-}
  EVIDENCE_HIGH_EARNER_THRESHOLD_NGN: ${EVIDENCE_HIGH_EARNER_THRESHOLD_NGN:-}
  EVIDENCE_RENEWAL_LOOKBACK_DAYS: ${EVIDENCE_RENEWAL_LOOKBACK_DAYS:-}
  EVIDENCE_CHALLENGE_RESPONSE_HOURS: ${EVIDENCE_CHALLENGE_RESPONSE_HOURS:-}
  EMAIL_PROVIDER: ${EMAIL_PROVIDER:-}
  EMAIL_SENDER_ADDRESS: ${EMAIL_SENDER_ADDRESS:-}
  EMAIL_SENDER_NAME: ${EMAIL_SENDER_NAME:-Cardvert}
  EMAIL_SMTP_HOST: ${EMAIL_SMTP_HOST:-}
  EMAIL_SMTP_PORT: ${EMAIL_SMTP_PORT:-1025}
  EMAIL_SMTP_USERNAME: ${EMAIL_SMTP_USERNAME:-}
  EMAIL_SMTP_PASSWORD: ${EMAIL_SMTP_PASSWORD:-}
  EMAIL_SMTP_STARTTLS: ${EMAIL_SMTP_STARTTLS:-false}
  EMAIL_RECEIPT_SIGNING_SECRET: ${EMAIL_RECEIPT_SIGNING_SECRET:-}
  EMAIL_RECEIPT_KEY_ID: ${EMAIL_RECEIPT_KEY_ID:-}
  BUDGET_POLICY_EXTERNAL_APPROVED: ${BUDGET_POLICY_EXTERNAL_APPROVED:-false}
  BUDGET_POLICY_ID: ${BUDGET_POLICY_ID:-}
  BUDGET_POLICY_REVISION: ${BUDGET_POLICY_REVISION:-}
  BUDGET_ALERT_RATIO: ${BUDGET_ALERT_RATIO:-}
  BUDGET_PAUSE_RATIO: ${BUDGET_PAUSE_RATIO:-}
  BUDGET_RESUME_RATIO: ${BUDGET_RESUME_RATIO:-}
  PHONE_OPERATOR_EXTERNAL_APPROVED: ${PHONE_OPERATOR_EXTERNAL_APPROVED:-false}
  PASSWORD_RESET_PUBLIC_URL: ${PASSWORD_RESET_PUBLIC_URL:-}

x-backend-service: &backend-service
  image: ${BACKEND_IMAGE:?BACKEND_IMAGE is required}
  environment: *backend-environment
  read_only: true
  tmpfs:
    - /tmp:rw,noexec,nosuid,size=64m
  secrets:
    - source: postgres_tls_ca
      target: postgres_tls_ca
    - source: redis_tls_ca
      target: redis_tls_ca
  cap_drop:
    - ALL
  security_opt:
    - no-new-privileges:true
  restart: unless-stopped
  networks:
    app: {}
    data: {}
    egress:
      gw_priority: 1

services:
  db:
    image: ${POSTGIS_IMAGE:?POSTGIS_IMAGE is required}
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - >-
        cp /run/secrets/postgres_tls_cert /run/cardvert-postgres/server.crt;
        cp /run/secrets/postgres_tls_key /run/cardvert-postgres/server.key;
        cp /run/secrets/postgres_tls_ca /run/cardvert-postgres/ca.crt;
        printf '%s\n'
        'local all all scram-sha-256'
        'hostnossl all all 0.0.0.0/0 reject'
        'hostnossl all all ::/0 reject'
        'hostssl all all 0.0.0.0/0 scram-sha-256'
        'hostssl all all ::/0 scram-sha-256'
        > /run/cardvert-postgres/pg_hba.conf;
        chown -R postgres:postgres /run/cardvert-postgres;
        chmod 0700 /run/cardvert-postgres;
        chmod 0600 /run/cardvert-postgres/server.key;
        chmod 0644 /run/cardvert-postgres/server.crt /run/cardvert-postgres/ca.crt
        /run/cardvert-postgres/pg_hba.conf;
        exec docker-entrypoint.sh postgres
        -c ssl=on
        -c ssl_cert_file=/run/cardvert-postgres/server.crt
        -c ssl_key_file=/run/cardvert-postgres/server.key
        -c ssl_ca_file=/run/cardvert-postgres/ca.crt
        -c hba_file=/run/cardvert-postgres/pg_hba.conf
        -c password_encryption=scram-sha-256
    environment:
      POSTGRES_DB: mobility
      POSTGRES_USER: mobility
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      PGPASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    tmpfs:
      - /run/cardvert-postgres:rw,noexec,nosuid,size=1m,mode=0700
    secrets:
      - source: postgres_tls_ca
        target: postgres_tls_ca
      - source: postgres_tls_cert
        target: postgres_tls_cert
      - source: postgres_tls_key
        target: postgres_tls_key
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          psql "host=db port=5432 dbname=mobility user=mobility sslmode=verify-full
          sslrootcert=/run/secrets/postgres_tls_ca" -Atc 'SELECT 1' | grep -qx 1
      interval: 5s
      timeout: 5s
      retries: 20
    restart: unless-stopped
    networks:
      - data

  redis:
    image: ${REDIS_IMAGE:?REDIS_IMAGE is required}
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - >-
        cp /run/secrets/redis_tls_cert /run/cardvert-redis/server.crt;
        cp /run/secrets/redis_tls_key /run/cardvert-redis/server.key;
        cp /run/secrets/redis_tls_ca /run/cardvert-redis/ca.crt;
        chown -R redis:redis /run/cardvert-redis;
        chmod 0700 /run/cardvert-redis;
        chmod 0600 /run/cardvert-redis/server.key;
        chmod 0644 /run/cardvert-redis/server.crt /run/cardvert-redis/ca.crt;
        exec docker-entrypoint.sh redis-server
        --appendonly yes
        --port 0
        --tls-port 6379
        --tls-cert-file /run/cardvert-redis/server.crt
        --tls-key-file /run/cardvert-redis/server.key
        --tls-ca-cert-file /run/cardvert-redis/ca.crt
        --tls-auth-clients no
        --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    volumes:
      - redis_data:/data
    tmpfs:
      - /run/cardvert-redis:rw,noexec,nosuid,size=1m,mode=0700
    secrets:
      - source: redis_tls_ca
        target: redis_tls_ca
      - source: redis_tls_cert
        target: redis_tls_cert
      - source: redis_tls_key
        target: redis_tls_key
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          REDISCLI_AUTH="$${REDIS_PASSWORD}" redis-cli --no-auth-warning --tls
          --cacert /run/secrets/redis_tls_ca -h redis -p 6379 ping | grep -qx PONG
      interval: 5s
      timeout: 5s
      retries: 20
    restart: unless-stopped
    networks:
      - data

  migrate:
    <<: *backend-service
    profiles: ["release"]
    command: ["alembic", "upgrade", "head"]
    restart: "no"
    depends_on:
      db:
        condition: service_healthy

  api:
    <<: *backend-service
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--no-access-log"]
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - >-
          from urllib.request import urlopen;
          r=urlopen("http://127.0.0.1:8000/api/v1/health/ready",timeout=6);
          raise SystemExit(0 if r.status == 200 else 1)
      interval: 30s
      timeout: 8s
      retries: 20
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  worker:
    <<: *backend-service
    command: ["arq", "app.jobs.worker_entry.WorkerSettings"]
    healthcheck:
      test: ["CMD", "arq", "--check", "app.jobs.worker_entry.WorkerSettings"]
      interval: 30s
      timeout: 10s
      retries: 5
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    image: ${FRONTEND_IMAGE:?FRONTEND_IMAGE is required}
    environment:
      API_BASE_URL: http://api:8000
      SESSION_COOKIE_NAME: ${SESSION_COOKIE_NAME:?SESSION_COOKIE_NAME is required}
      LOGIN_RATE_LIMIT_RELAY_CLIENT_IP_HEADER: ${LOGIN_RATE_LIMIT_RELAY_CLIENT_IP_HEADER:-true}
      DEMO_LOGIN_ENABLED: "false"
      SENTRY_DSN: ${SENTRY_DSN:-}
      RELEASE_REVISION: ${RELEASE_REVISION:?RELEASE_REVISION is required}
      NODE_ENV: production
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=32m
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - >-
          fetch("http://127.0.0.1:3000/login").then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))
      interval: 5s
      timeout: 3s
      retries: 20
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    networks:
      edge: {}
      app:
        ipv4_address: 10.255.254.10
      egress:
        gw_priority: 1

  edge:
    image: ${CADDY_IMAGE:?CADDY_IMAGE is required}
    environment:
      EDGE_HOSTNAME: ${EDGE_HOSTNAME:?EDGE_HOSTNAME is required}
      RELEASE_REVISION: ${RELEASE_REVISION:?RELEASE_REVISION is required}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    healthcheck:
      test: ["CMD", "caddy", "validate", "--config", "/etc/caddy/Caddyfile"]
      interval: 30s
      timeout: 5s
      retries: 3
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
      frontend:
        condition: service_healthy
    networks:
      edge: {}
      app: {}

volumes:
  postgres_data:
  redis_data:
  caddy_data:
  caddy_config:

secrets:
  postgres_tls_ca:
    file: ${POSTGRES_TLS_CA_FILE:?POSTGRES_TLS_CA_FILE is required}
  postgres_tls_cert:
    file: ${POSTGRES_TLS_CERT_FILE:?POSTGRES_TLS_CERT_FILE is required}
  postgres_tls_key:
    file: ${POSTGRES_TLS_KEY_FILE:?POSTGRES_TLS_KEY_FILE is required}
  redis_tls_ca:
    file: ${REDIS_TLS_CA_FILE:?REDIS_TLS_CA_FILE is required}
  redis_tls_cert:
    file: ${REDIS_TLS_CERT_FILE:?REDIS_TLS_CERT_FILE is required}
  redis_tls_key:
    file: ${REDIS_TLS_KEY_FILE:?REDIS_TLS_KEY_FILE is required}

networks:
  edge:
    internal: false
  app:
    internal: true
    ipam:
      config:
        - subnet: 10.255.254.0/24
  data:
    internal: true
  egress:
    internal: false
