# Client Decisions — Mobility AdTech Platform

The single decisions doc, in two parts. **Part 1** is the append-only log of
confirmed product decisions (D-numbers) in the order they were made — newer
rows supersede older ones where they conflict; never edit history. **Part 2**
is the *current* status of every questionnaire item Q1–Q34 with its adopted
direction and divergence guard (formerly `docs/adopted-decisions.md`, merged
4 Aug 2026 — stale references to that filename resolve here).

How change flows through the doc system: client decisions change **here**
(new Part 1 row + Part 2 status flip) → `docs/architecture.md` is amended in
the same commit → agents build to the amended architecture → delivered work
is recorded in `docs/progress.md`. Scope baseline: the client proposal
(`docs/Mobility_AdTech_MVP_Proposal_5_Month_Retargeting.docx`, D11), as
superseded by later direct client and project-owner decisions—currently D18–D25—where they
conflict.

## Part 1 — Decision log (append-only)

| # | Date | Decision | Source | Build implication |
|---|------|----------|--------|-------------------|
| D1 | Jul 2026 | **Onboarding is operator-led** — no public self-serve advertiser signup; admin creates users/orgs. | Backend design + questionnaire v1 | Admin "create user + org" flow (built, F5). Driver self-registration is a separate open question (Q13 in questionnaire v2). |
| D2 | Jul 2026 | **Driver pay = fixed naira amount per hour**, not a revenue share and not per-km rate cards. | Somto, via refined questionnaire (Q4 preamble) | Payout engine reworked: hourly rate × verified payable time replaces the per-km + zone-bonus + impression components. Zones shift from pay-bonuses to campaign rules & analytics inputs. Rate flexibility (one rate vs per-campaign override) still open → Q4. |
| D3 | Jul 2026 | **MVP tracking = screen-on tracking** with the installable driver app (phone mounted, app visible — navigation-app posture). Native app with true background tracking comes after the pilot; identical backend contract. | Somto, via OJ | No native-app build blocks the pilot. Driver UX copy says "keep the app on screen while driving". Q10's Start/End session model carries over unchanged to the native app later. |
| D4 | Jul 2026 | **Payable hours are capped** — each campaign sets a max payable hours per driver per day (admin-configured, shown in the driver's offer before acceptance). | Somto, via OJ | Earnings calculation gains a per-campaign daily cap; driver app shows progress toward the cap; campaign cost becomes quotable (cap × rate × vehicles × days). |
| D5 | Jul 2026 | **Fraud posture = hold-and-review**, not automatic permanent pay discounts: seriously flagged sessions hold earnings until admin review; repeat/severe flags can pause new assignments; drivers get the reason and a dispute channel. Thresholds stay configurable, tuned with pilot data. | Refined questionnaire (Q21 rule, pending formal approval) | Fraud-review workflow (acknowledge/resolve endpoints) becomes a required backend addition. The old severity multipliers (0.9/0.7/0.25) become secondary/configurable rather than the primary mechanism. |
| D6 | Jul 2026 | **Offline-to-online retargeting enters the MVP** (shape still open → Q11: dashboard-only vs anonymised exportable segments vs direct platform integrations). | Refined questionnaire | New MVP workstream once Q11 lands: audience aggregation model + export/activation flow + privacy controls. Was "future layer" in the original brief. |
| D7 | Jul 2026 | **In-platform creative upload is an MVP rule** (pending approval, Q18 rule): advertisers upload files; admin approves/rejects before production. | Refined questionnaire | File upload + storage + review queue becomes a backend/frontend addition (was deferred in v1 questionnaire). |
| D8 | 27 Jul 2026 | **Questionnaire v2 resolved by adopted defaults** — client unresponsive, so best-practice industry defaults were adopted for every Q with a defensible standard answer. Q23 (owner-drivers) is CONFIRM-PENDING (put to the client as a named yes/no); OPEN remain: Q26 (legal wording only), Q28 (company facts only), Q29 (brand), Q30 (pilot shape), Q31 (privacy sign-off), Q32 (client account action only), Q33 (ops owner). Per-Q statuses, adopted directions, and mandatory divergence guards live in **Part 2 of this file** (originally `docs/adopted-decisions.md`, merged here 4 Aug 2026). Client-facing copy: `docs/Mobility_Working_Decisions_and_Open_Items.docx`, which supersedes the questionnaire as the client artefact. | Developer-adopted defaults, 27 Jul 2026 (adversarially reviewed + reconciled per SOP) | The answers-blocked sprint unblocks: build to ADOPTED rows as if confirmed, implementing each row's divergence guard. D5/D7 "pending approval" caveats are resolved (Q21/Q18 adopted); D1 is narrowed by Q13 (drivers self-register; operator-led stays for advertisers/orgs). A late client reversal = superseding Part 1 row + Part 2 status flip + architecture amendment in the same commit. |
| D9 | 30 Jul 2026 | **Payout v2 delivered (S1) with these frozen money policies:** (a) `payout_v2` quantizes once per ledger amount at 2dp NGN with ROUND_HALF_UP; `payout_v1` history keeps its original HALF_EVEN (`quantize_2`) — the two never converge (changing v2 rounding means `payout_v3`). (b) Pings with NULL reported accuracy fail the eligibility accuracy gate (pay demands affirmative signal quality). (c) "Approved area" (Q5) = inside a `target` zone (campaigns with no target zones pay everywhere) minus `exclusion` zones; `bonus` zones play no payout-v2 role (D2 demoted zones from pay-bonuses to rules). (d) The platform default hourly rate (Q4) is config (`PAYOUT_DEFAULT_HOURLY_RATE_NGN`), ships unset (0 = rate required per rule); the resolved rate lives on the rule row — the Q4 divergence guard is the config fallback mechanism, exercised in tests. (e) The v1 `min_payout_per_trip` floor lifts only trips with zero open fraud flags (§17 loophole closed; extend to `confirmed` when S2 adds it). (f) `payout_v2` calculations are write-once per trip: input drift is flagged (409 on admin recompute), never auto-recomputed; the audited admin recompute-day true-up is the only corrective path, emitting append-only `adjustment`/positive-`reversal` differentials — and its reallocations are authoritative for later same-day cap accounting (the differential entry's stored `payable_seconds` supersedes the calculation's). (g) Recompute-day prices `insufficient_data` trips from a fresh classification (the admin escape hatch for healed analytics); `blocked` trips stay at zero pending the S2 fraud review; days whose calculations mix formula versions or currencies are refused (409), and the tool prices at the governing rule's current terms — that re-pricing is its purpose (rate-correction true-ups). | Developer (S1 build, SOP plan + adversarial review) | See `architecture.md` §16.1 [BUILT] + v1.7 changelog. Supersede any item here only via a new row. |
| D10 | 3 Aug 2026 | **Data lifecycle delivered (S4) with these frozen policies:** (a) `location_pings` partitions are **UTC month** ranges on `recorded_at` (`captured_at` never existed — architecture amended); conversion was rename-and-attach, so ping ids (and the payout fingerprints embedding them) survived byte-for-byte. (b) **No default partition, ever** — premake + a two-detector coverage alarm (worker Sentry check + `GET /api/v1/health/partitions` 503) own the write-outage risk; the migration's initial premake horizon is frozen at 4 months and never reads deployment config. (c) Purge evidence = append-only **lifecycle-event rows** in `data_purge_audit` (never UPDATEd); the `dropped` row commits in the same transaction as `DROP TABLE`; one `dropped` row per partition is DB-enforced (an insert conflict fails the drop closed); interrupted detaches recover via FINALIZE only when purge evidence exists AND the partition is still retention-expired — otherwise the run refuses, alerts, and skips all destruction (a pending partition is invisible through the parent, so purging around it would cascade-delete retained pings); unclaimed tables are never dropped. (d) Ping batches are purged only when **zero pings remain** and the batch is older than the retention window — a straddling batch keeps its newer pings; recent zero-ping batches keep serving idempotent replays. (e) **Ping-batch ingestion is audit-exempt** (orchestrator-approved architecture exception): `location_ping_batches` is the compensating ingestion evidence; idempotent replays create neither mutations nor audit events; the exemption is a named, reasoned entry in the route-coverage test. (f) The legacy partition over-retains its oldest months until its whole range expires — bounded, deliberate, documented. (g) Residual audit gaps discovered but out of S4 scope (auth.refresh, driver profile self-update, driver assignment accept/activate/deactivate) are registered KNOWN_UNAUDITED in the coverage test — follow-up work. | Developer (S4 build, SOP plan + adversarial review + orchestrator approval) | See `architecture.md` v1.9 changelog; §24.2 [BUILT]. Supersede any item here only via a new row. |
| D11 | 4 Aug 2026 | **The client-facing 5-month MVP proposal is the binding scope baseline** (`docs/Mobility_AdTech_MVP_Proposal_5_Month_Retargeting.docx`). Every planning doc must match its full promised scope; narrower scope statements in older internal docs are historical and superseded. Concretely: (a) **The driver mobile app is inside the MVP window** (React Native or Flutter; background GPS tracking; offline/retry route sync; push notifications where required). This supersedes the *timing* of D3's "native app phase 2 / commissioned separately" — the delivered screen-on PWA remains the interim tracking surface, and D3's identical-backend-contract promise stands (§23's refresh-credential + FCM design is the implementation path). (b) **Online-to-offline retargeting ships at full proposal Module G scope**: retargeting source records (website traffic, digital campaign audiences, CRM/upload references, UTM campaign sources, manually supplied audience/location insights), source↔campaign/target-zone linkage, privacy-safe segment metadata, follow-up-targeting insights inside advertiser reports and dashboards, and admin retargeting monitoring. This *extends* — does not replace — Q11's anonymised exposure-segment model and the §22 privacy boundary; segment export stays gated on Q31; automated Meta/Google/TikTok activation stays out of the MVP unless the client supplies accounts, legal approval, API access, and budget (the proposal's own condition). (c) **Reaffirmed in-scope MVP deliverables**: basic CSV/PDF export, admin fraud/anomaly views, pilot deployment, onboarding/training materials, and a post-MVP roadmap. | OJ — client proposal sent Aug 2026 | Realignment recorded across `architecture.md` (§2.2, §3, §22, §23, §30, §31, A2 + v1.10 changelog), the Q11 status row (Part 2), historical banners on `docs/build-loop/README.md` + `product-brief.md`, the progress doc, and root `README.md`. Agents plan against the proposal's full scope. |

| D12 | 4 Aug 2026 | **Hourly pay model reconfirmed against the proposal's Module E wording**: total earnings = hourly rate × verified payable hours (Q5 eligibility rules define "verified"; D4 per-campaign daily caps apply). The proposal's "mileage-based earnings" phrasing is requirement-era wording superseded by D2/D9 — mileage, zones, and exposure quality act as *verification and analytics inputs*, not rate components. Client-facing wording to be aligned at the next proposal/comms revision (OJ, not agents). | OJ, 4 Aug 2026 | Resolves architecture R9. No build change — `payout_v2` (S1) already implements exactly this. D2/D9 frozen policies stand; any future move to per-km pay = new D-row + `payout_v3`. |

| D13 | 6 Aug 2026 | **Independent review reconciled; 18 remediation rows adopted, 4 findings rejected.** Two external reviews (broad architecture + money-path red team) were run on the doc packet, then every code-checkable claim was **verified against the implementation** before acceptance. Adopted findings live in `architecture.md` **§35** with gates; do not re-litigate them there. **REJECTED with reason (do not re-raise):** (1) *"Client GPS timestamps can be stretched/shifted into payable windows"* — refuted: `trip_sessions.started_at/ended_at` are server-generated (`services/trips.py:151,184,265-267`), pings carry both `recorded_at` and server `received_at` (`models/trip.py:216-219`), `ensure_ping_bounds` rejects >5 min future and >15 min pre-start (`trips.py:325-340`), pings are accepted only while ACTIVE, and the classifier clamps every offset into the server envelope (`payout_eligibility.py:149-154`) so eligible seconds can never exceed real session time. (2) *"One-campaign-per-vehicle has no DB constraint"* — refuted: `uq_campaign_assignments_vehicle_active` and `uq_campaign_assignments_campaign_vehicle_non_terminal` exist (migration `0006:100-113`). (3) *"No DB constraint prevents multiple active trips"* — refuted: `uq_trip_sessions_driver_profile_active` and `uq_trip_sessions_vehicle_active` exist (migration `0007:102-115`). *(2) and (3) leave only the minor error-mapping gap kept as RM7.* (4) *"Post-payment reversals are created `paid` and never recovered"* — downgraded, not rejected: no `paid` status and no payout run exist today (statuses are `pending/available/voided/reversed`), so it is an S3 specification requirement (RM11), not a live defect. **Two defects the reviewers could not see were found during verification and adopted as RM4 and RM5**: the PWA mints a fresh idempotency key per retry (double-insert on lost response — this also invalidates the red team's "batch replay is prevented by design" row), and the ping buffer is in-memory only, so a reload loses unsent points. Two findings are **worse** than reported: sub-5-minute stays are never excluded at all (RM2), and post-end batches are rejected outright rather than queued (RM3). | Independent review + code verification, 6 Aug 2026 | §35 is the authoritative remediation list with gates; §31 waves resequenced (RM17). No scope change — D11's promised scope is unchanged; this is correctness and sequencing work inside it. Supersede any row only via a new decision. |

| D14 | 6 Aug 2026 | **The `payout_v2` freeze binds from the first real payout, not from today — so RM1 is fixed in place as v2, not as `payout_v3`.** D9(a)'s version-freeze exists to keep *real historical money facts* reproducible and explainable in a dispute. **There are none:** nothing is deployed, no payout run exists in code, no `paid` status exists, and the only ledger entries ever marked `available` come from the demo seed (regenerated on every re-seed). RM1's cross-midnight cap allocation is therefore a **bug fix against D4's stated intent** (D4 has always said the cap is per *calendar day*; the code keyed it to the trip's start day), not a change of pay policy — it ships inside `payout_v2`. Binding rules going forward: (a) the freeze starts at the first payout computed for a real driver on real tracked data — from that moment any change to how v2 computes money requires `payout_v3`; (b) a genuine *policy* change (rate basis, rounding, what counts as payable) requires a new formula version regardless of timing; (c) a correction of an implementation that provably contradicts a recorded decision is a bug fix inside the current version, and must cite the decision it restores. | OJ, 6 Aug 2026 | RM1 implemented in `payout_v2`: per-Lagos-day interval allocation in the classifier, per-day cap accounting, per-day locks, persisted allocation for recompute reproducibility. Demo-seed money values may change on re-seed — expected and harmless. |
| D15 | 9 Aug 2026 | **Trip finality protocol adopted (RM3/RM4/RM5): a trip's money computes only after it is `sealed`.** Frozen shapes: (a) lifecycle `active → ended → sealed`, one-way, guarded-UPDATE transitions, every seal audited once (`trip.sealed`); (b) the **seal predicate is the batch-count watermark** — the server holds ≥ `client_batch_count` location-ping batches; `client_ping_count` and `client_complete` are diagnostic evidence only, and a satisfied count seals early even after a `client_complete=false` end (tracking stops at end, so the count is final — waiting for grace would buy no safety); (c) unsatisfied/absent watermarks seal by worker sweep after ⚙ `TRIP_SEAL_GRACE_SECONDS` (default 600) with reason `grace_expired`; (d) `ended` is the late-data recovery window — batches are accepted, the assignment-active gate is deliberately skipped (evidence delivery must not depend on assignment state), and `recorded_at` is bounded by ⚙ `LOCATION_PING_END_SKEW_SECONDS` (300 — matches the future-skew tolerance so a fast client clock cannot poison its own final batch); (e) post-seal batches are **quarantined, never rejected**: evidence-preserving rows with the same idempotency semantics as live batches, resolved only by audited admin apply/discard with a mandatory note; **apply never auto-recomputes money** — payout_v2 is write-once and the recompute-day tool (S1) is the sole corrective path, so the apply response names the affected Lagos days; (f) quarantine payloads are raw location data and purge on the same §24.2 retention window with `quarantined_batches_purged` evidence; (g) the client queue is IndexedDB-durable with idempotency keys minted once at batch-cut time and reused across every retry; terminal rejections (400/409/422) dead-letter the batch; one tab tracks at a time (Web Locks). Report surfaces fold `sealed` into `ended` — the seal is an internal money-integrity state. | Developer (W0 slice, SOP plan + independent adversarial review + reconcile) | Architecture v1.16; §35 RM3/RM4/RM5 rows closed; migration `0016` frozen once shipped. The D18 pilot PWA and later Phase 2 native app inherit this exact contract. Supersede only via a new row. |
| D16 | 9 Aug 2026 | **Independent post-implementation review of the D15 slice adopted in full (10 findings; RM3/RM5 briefly reopened, re-closed same day).** Two invariants are added to the D15 protocol: (a) **quarantine apply requires the trip's initial payout calculation to exist** — a sealed-but-unprocessed trip rejects apply (`QUARANTINE_APPLY_BLOCKED`), so whether post-seal evidence reaches money never depends on admin-vs-worker timing; recompute-day remains the only corrective money path; and (b) **analytics computed before `sealed_at` are never reused for money** — the pipeline recomputes them over the sealed ping set. Client posture is fail-closed: no durable storage or no single-tab lock means no tracking and no ending from that tab, and the end watermark claims completeness only when a healthy queue is fully drained. Guarded transitions cover `active → ended` as well as `ended → sealed`; quarantine resolution is row-locked with DB CHECK constraints; 0016's backfilled seals received their audit events retroactively (migration `0017`). | Independent reviewer + developer rework, 9 Aug 2026 | Architecture v1.17; §35 RM3/RM5 rows carry the hardening notes. Supersede only via a new row. |
| D17 | 12 Aug 2026 | **One encryption-provider boundary owns pilot bank data and later KYC/national identifiers.** MNY-10A introduces a P5 crypto port (`encrypt`, `decrypt`, `rotate`) using authenticated per-record envelope encryption. The pilot KEK is required typed Settings with no default; every encrypted value carries algorithm and key version and binds tenant/record/field as associated data. W2-02D replaces/extends key custody with the selected KMS/vault provider and supplies rewrap/re-encryption without changing ciphertext schema. Plaintext national/financial values never enter DB rows, list APIs, logs, audit payloads, exports, errors, or fallback storage. | Project owner delegated to documented best practice, corrections brief 12 Aug 2026 | Target/unbuilt decision. MNY-10A owns the port and pilot provider; W2-02D owns KMS/vault custody adoption and migration. Both require ciphertext inspection, rotation/rewrap, associated-data failure and recovery tests. |
| D18 | 14 Aug 2026 | **Somto transmitted the client's complete Q1-Q34 product direction; these are direct client answers, the newest product authority, and they supersede every earlier D-row, adopted default or proposal interpretation where they conflict.** Part 2 records the answer-by-answer truth. Material supersessions are: (a) Q5 keeps fixed naira-per-hour pay but makes campaign geography a price tier: valid campaign time outside the primary/premium zone earns the rule's base hourly amount, while time inside the primary/premium zone earns its disclosed premium hourly amount; exclusion zones and invalid GPS/time remain unpaid. This supersedes D2/D12/D9(c)'s claim that zones never affect price, not the fixed-hourly basis, and is a genuine policy change requiring `payout_v3` under D14. (b) Q10 restores the screen-on installable PWA as the pilot driver client and moves native background tracking after the pilot, superseding D11's native-in-MVP timing while preserving Start/End and the identical backend-contract promise. (c) Q22 makes clean/current-successful assessments available for the next weekly batch without a universal T+7 wait; only flagged/under-review earnings are held for a seven-day review SLA, and unresolved cases remain held and escalate rather than auto-release. (d) Q24 permits an advertiser refund only within 24 hours of the first confirmed cash allocation that authorizes production; after that cutoff there is no advertiser refund, while cancellation still stops new work and preserves verified driver earnings to the cutoff. (e) Q27 requires automated bank transfers through an approved provider for the pilot, behind the RM10/RM11 batch/finality controls. Confirmed names and pilot facts are: business **Terrax Media**, product/app **Cardvert**, Abuja, 10 vehicles, 5 paying advertisers, 3 months, with campaign ROI analysis, offline-to-online targeting and a 60% target-area coverage measure among the success criteria. | Direct client answer key transmitted by Somto, 14 Aug 2026 | Amend architecture and future package contracts before implementation. `payout_v2` remains immutable built history/demo evidence; PKG-01 MNY-06B owns the `payout_v3` rule-binding consequence. R14-A/B and W4-01 become production PWA proof/delivery; native credentials, background modes and store release move post-pilot. Provider-neutral work remains runnable, but live disbursement/direct ad activation require their registered external inputs. Q30 permit evidence, Q26/Q31 legal artifacts, Q28 statutory invoice facts and Q32 account/domain/budget action remain external. |
| D19 | 14 Aug 2026 | **Abuja pilot permit governance:** Terrax Media owns confirmation of applicable vehicle-advertising permits; approved platform-managed vendors coordinate acquisition and evidence. No pilot campaign launches until the permit evidence is recorded and approved. | Project owner, 14 Aug 2026, adopting the requested Q30(F) recommendation | This assigns responsibility but does not invent or waive the permit. `EXT-PILOT-PERMITS` remains missing until actual Abuja evidence/approval exists. |
| D20 | 14 Aug 2026 | **Client-approved implementation clarifications for Q11, Q24 and Q30.** (a) Q11 pilot activation is controlled export plus approved geographic/time/contextual ad-platform targeting; route/exposure data must never be represented or submitted as a person-level audience, and person-level exposure retargeting requires suitable lawfully obtained identity data, approval and a later approved identity/location-data partner. (b) Q24 standard production waits until the 24-hour refund window closes; an advertiser may request expedited production only through an immutable audited waiver, and refund eligibility ends when waived production actually begins. (c) Q30's standard deliverable is **Campaign Performance Analysis**. Financial ROI appears only when an advertiser supplies defined conversion/revenue data and an approved reproducible methodology exists; otherwise ROI is omitted, not estimated or implied. | Direct client approval of the three-point implementation clarification, relayed by project owner, 14 Aug 2026 | Refine existing contracts only: no new package, checklist item or dependency. W2 commercial authority owns the production wait/waiver; W3/W4 reporting fails closed on ROI inputs/method; W3 activation accepts aggregate geography/time/context only. D18 remains immutable history and is interpreted through this later approval. |
| D21 | 20 Aug 2026 | **MNY-06 payout-v3 and correction-authority implementation contract.** D18/Q5 is implemented with immutable effective-dated rule revisions and acceptance-time bindings that freeze rates, daily cap, complete resolved eligibility values and premium/exclusion geometries. Valid time outside premium geography earns base; valid time inside earns premium; exclusions/invalid time are unpaid. The one shared Lagos-day cap fills chronologically across v2/v3, and base/premium components are persisted so their sum equals the once-quantized authoritative ledger amount. Historical change is permitted only through a projected correction order with a separate approver, stale-fingerprint recheck and idempotent execution; positive deltas carry their own release time. | PKG-01 implementation plus independent money/security and architecture/concurrency reviews | Freezes the deterministic D4/D18 implementation semantics delivered by MNY-06A/B/C. Later rule, zone or settings changes never reprice accepted work; payout-v1/v2 history stays immutable. Supersede only with a new decision/formula version. |
| D22 | 20 Aug 2026 | **Parked-time payment policy selected for build-first completion.** New acceptances use the reviewed synthetic Option A: contiguous 120-second elapsed observation windows, net endpoint displacement at or below 25 metres is stationary, two adjacent stationary windows confirm and backdate to the first qualifying window, one above-threshold window releases and backdates, and detector state resets per trip and after a GPS gap. The existing 200-metre long-stay radius, 300-second stay window, 240-second whole-trip grace, 75-metre accuracy ceiling, 180 km/h teleport threshold and 120-second maximum ping gap do not change. The selected values are provisional and may be tuned after real-route calibration only through a new effective revision for later acceptances; accepted work and payout-v1/v2 history are never silently repriced. | Project owner sealed successor-controller handoff, 20 Aug 2026, adopting the independently reviewed synthetic candidate | FND-02A is decided and `EXT-RM2-POLICY` is present. FND-02B binds the complete policy and marker at acceptance, fails closed for unresolved markers, persists auditable reason/evidence and explains the result to drivers/admins. `EXT-RM2-CALIBRATION-DATA` remains useful post-build evidence but blocks no build item. |
| D23 | 20 Aug 2026 | **Build completion precedes unavailable real-world validation.** A package may close when its product behavior, automated unit/integration/contract tests, deterministic synthetic end-to-end evidence, required reviews, authority documents and exact CI agree. Missing real-device, real-route, battery, live-staging, pilot/operator or user-feedback evidence must remain explicitly incomplete post-build validation; it must never be represented as completed evidence and cannot by itself keep an otherwise complete build item/package open. All existing real-GPS, release and pilot safety gates remain fail closed. | Project owner programme rule and sealed successor-controller handoff, 19–20 Aug 2026 | R14-A/R14-B now close on capability-contract and interrupted synthetic-flow build evidence; representative Android/iPhone route/battery validation remains required before the production-PWA pilot gate. R17-A closes on provider-neutral production-like topology/release/recovery build evidence; deployment and restore in an approved external environment remain required before W4 release/pilot. No external approval is invented or waived. |
| D24 | 24 Aug 2026 | **Advertiser transactional email is enabled by default at advertiser-organization scope and may be disabled account-wide; in-app notifications remain mandatory.** Active organization members observe the same preference, only existing authorized organization managers may change it, and every effective change records the actor plus before/after state. This preference governs transactional delivery only; it does not create marketing consent or authorize a provider. | Project owner, accepting the independently reviewed W2-04A recommendation | W2-04A stores one preference per advertiser organization, keeps in-app unsuppressible and audits changes. W2-04B must consult this organization preference before creating/sending email rows; `EXT-EMAIL-PROVIDER` still gates provider delivery. |
| D25 | 1 Sep 2026 | **Exact signed v2 trip-evidence authority supersedes D15's count/grace seal predicate for new trips.** A v2 trip commits one immutable ordered manifest of batch sequence, idempotency key, canonical payload hash/version and submitted count. Only exact descriptor reconciliation may mark that manifest complete and seal the trip; omission, reorder, duplication, undeclared batches, changed content, false completeness, under/over-count and grace expiry never seal or originate money. Each accepted/quarantined batch and verified manifest receives a content-bound HMAC-SHA-256 receipt under a dedicated versioned signing keyring; referenced verification keys remain available through rotation and readiness fails closed when authority is unavailable. Ended-trip delivery remains permitted after assignment deactivation only for exact precommitted descriptors; new capture still requires active authority. Existing v1 rows remain readable and existing money facts repairable, but v1 cannot originate new money. | R34/OFF-001 reviewed remediation contract, project owner authorization, 1 Sep 2026 | Migration `0074` adds the immutable manifest/receipt schema and guarded downgrade. The browser and Python share one canonical v2 encoding and golden vectors; the durable queue retains signed receipts and submits the exact manifest before reconcile. Grace becomes an audited expiry marker only. D15/D16 remain historical authority for lifecycle, quarantine, retention, locking and corrective-money behavior except where this later row replaces count/grace completeness. No native/device/live-use authority is implied. |
| D26 | 2 Sep 2026 | **Logout revokes the user's sessions on every device.** A successful logout must rotate durable session authority, clear the current browser cookie, make every previously issued bearer/refresh capability unusable, and prevent a concurrently completing refresh from restoring any session. | Direct project-owner/developer decision in the Cardvert remediation controller, 2 Sep 2026 | R11/AUT-004 implements global revocation using the existing `session_version` authority and updates user-facing/runbook wording. No per-device session model or migration is required. Ordinary authenticated use before logout remains unchanged. |
| D27 | 2 Sep 2026 | **Developer security/offline/infrastructure defaults are decided.** (a) Privilege elevation requires password reauthentication and global session revocation before the elevated role is usable. (b) After an ambiguous trip-End response, the client stops capture, retains evidence, and retries the identical End request until authoritative confirmation; it does not create new post-End capture. (c) Public registration abuse control uses per-IP and normalized-identity limits, non-revealing responses, and operational alerts rather than a global availability switch. (d) Pilot PostgreSQL/Redis may be bundled or managed, but any production topology requires TLS, authentication, explicit hostnames, and externally supplied secrets. (e) Numeric code-coverage policy remains parked until current runtime product/engineering defects are complete. | Direct project-owner/developer selection of the recommended options in the Cardvert remediation controller, 2 Sep 2026 | `AUT-006`, `OFF-008`, `ONB-008`, and `REL-007` may receive current-source verification and newly reviewed bounded implementation packets; they are no longer decision-blocked. R17/TST-007 remains parked rather than blocking runtime remediation. No provider, credential, hostname, threshold value, deployment, or external evidence is invented by this decision. |
| D28 | 2 Sep 2026 | **Approved driver accounts use administrator-authorised, driver-completed activation.** After the required person/payee and vehicle decisions succeed, an authorised Cardvert administrator initiates activation for the linked invited driver. The driver receives a short-lived, single-use setup link and chooses the initial password; successful completion atomically activates the account and rotates session authority. | Direct project-owner/developer selection of the clarified recommended ONB-009 option in the Cardvert remediation controller, 2 Sep 2026 | ONB-009 may receive a current-source reviewed implementation packet. Rejected, expired or superseded applications cannot activate; setup replay fails; prior onboarding access and sessions cannot survive completion. Provider-neutral queued delivery may be built, but no live email/provider or credential is claimed or invented. |
| D29 | 2 Sep 2026 | **Advertiser logins are single-company, while one Cardvert admin may complete all driver approval checks.** An advertiser login may hold active membership in exactly one advertiser organization; multi-company agency access and automatic tenant selection are not supported. Separately, one active Cardvert administrator may verify the payout bank account and approve the same driver's person/payee and vehicle evidence. | Direct project-owner/developer clarification of AUT-007 and ONB-005 in the Cardvert remediation controller, 2 Sep 2026 | AUT-007 requires current-source enforcement of one active advertiser-organization membership per login, including existing conflicting rows, retries and races. ONB-005 retains the current single-admin trust boundary; no maker-checker rule is added, but each verification and decision must remain attributable in immutable audit/evidence. Cardvert platform administrators remain outside advertiser membership selection. |
| D30 | 2 Sep 2026 | **Duplicate driver identity/contact/payment matches are hard conflicts, and planning-source links freeze with terminal campaigns.** A new driver application is rejected when its NIN, normalized phone number, or payout bank account matches an existing driver. Advertiser planning-source links may be created or changed while a campaign is draft, pending, approved, scheduled, active, or paused; completed, cancelled, and rejected campaigns retain read-only historical links. | Direct project-owner/developer answers to ONB-003 and AUD-006 in the Cardvert remediation controller, 2 Sep 2026 | ONB-003 and AUD-006 leave the parked decision list and may receive current-source reviewed enforcement. Duplicate matching must use the repository's protected/normalized representations and fail without exposing another driver's identity. Terminal campaign history must not be rewritten; ordinary authorized edits in nonterminal states remain supported. |
| D31 | 2 Sep 2026 | **Vehicle approval ends on an administrator-entered date.** Drivers submit the relevant vehicle documents and an authorised Cardvert administrator reviews them, then explicitly selects the approval end date. That date may be later than a submitted document's expiry and is not automatically capped at 12 months. | Direct project-owner/developer clarification of ONB-004 in the Cardvert remediation controller, 2 Sep 2026 | ONB-004 leaves the parked decision list and may receive current-source reviewed enforcement. The selected end date and approving administrator must be durably attributable; work eligibility closes when the chosen approval date expires and renewal requires a new reviewed decision. Document expiry remains visible evidence but does not automatically override the administrator's selected horizon. |
| D32 | 5 Sep 2026 | **Changed executable code is coverage-ratcheted.** For the explicit full-SHA ancestor selected by the CI/local caller, changed eligible `app/**/*.py` and `frontend/src/**/*.{ts,tsx}` executable lines require at least 90% line and 80% branch coverage. The exact committed global and named-critical LCOV baselines may not regress. Generated code, tests, fixtures, migrations, build output and vendor code are excluded. Missing, malformed or conflicting coverage inputs, an invalid/non-ancestor base, or an unreported eligible changed source fail closed; adds, renames, deletes and no-change runs are reported explicitly. | Project owner’s R17 Option A, independently reviewed coverage-policy contract, 5 Sep 2026 | Supersedes **only D27(e)**. R17 adds pinned backend/frontend reporting and a standard-library policy checker; it neither changes D27(a)–(d) nor invents a provider, deployment, threshold exception or baseline update process. |

## Part 2 — Current Q1–Q34 statuses (merged from `adopted-decisions.md`, 4 Aug 2026)

**Status: CLIENT-CONFIRMED (D18–D20, 14 Aug 2026). Build against this Part.**

D8's adopted defaults unblocked the build while Somto's response was pending.
Somto transmitted the client's complete answer key in D18, and D20 records the
client's approved implementation clarification for Q11/Q24/Q30. Together they
supersede affected defaults while preserving the divergence-safe architecture
where it still fits. **This Part is the agent-facing truth for every Q-number.** Architecture
§33 routes each Q to its owning section; this Part owns its current status and
direction. A confirmed policy may still have missing live-use facts, which are
called out in the row and in `docs/progress.md`'s external register.

### Status taxonomy

| Status | Meaning | What you may do |
|--------|---------|-----------------|
| **CONFIRMED** | Client-confirmed (Somto), directly or via D1–D7. | Build to it. Supersede only via a new decisions-log row. |
| **ADOPTED** | Best-practice default adopted (D8). Client has been told "we build to this unless you flag it". | Build to it as if confirmed — do **not** block on the client. You MUST implement the row's **divergence guard** so a late client reversal is a config/data change, not a rework. |
| **CONFIRM-PENDING** | Explicitly put to the client as a named yes/no. | Build the stated base case; the named structural escape hatch must be preserved. |
| **OPEN** | Only the client can answer (facts, authority, or commercial terms). | Old `[OPEN]` rule applies: smallest thing that works under all still-open options; flag foreclosing designs (architecture P10). |

**Rules for agents**

1. Commit messages implementing one of these cite the Q-number and current
   governing D-row (`D18`, plus `D20` for Q11/Q24/Q30), plus any
   implementation-specific D-row.
2. A future client change appends a superseding row to Part 1, updates the Q
   row here, and amends `architecture.md` in the same change (architecture §1).
3. An ADOPTED row never weakens an architecture invariant. Where this file and
   `architecture.md` conflict, the architecture doc wins — flag the conflict
   instead of building.

### A. Core product decisions (Q1–Q14)

| Q | Topic | Status | Current direction | Build implication & divergence guard |
|---|-------|--------|-------------------|--------------------------------------|
| Q1 | Pricing structure | **CONFIRMED** (D18) | A custom quotation is prepared for every campaign; a launch package catalogue is not client-approved scope (§15). | Quote components, line items, terms and revisions are configurable data, never code; every accepted campaign preserves its immutable quotation snapshot. |
| Q2 | Payment timing | **CONFIRMED** | Standard advertisers pay in full before printing or installation begins; specifically approved corporate advertisers may use accepted invoice/credit terms (§15). | The invoice/receipt model supports both. Corporate credit never bypasses RM12: driver liability still requires an approved funding/subsidy/credit authorization before work. |
| Q3 | Payment methods | **CONFIRMED** | Both bank transfer with admin reconciliation and online gateway payment, in one payment history (§15.3). | Both paths converge on canonical receipts/allocations. Provider choice/account remains external. |
| Q4 | Hourly-rate uniformity | **CONFIRMED** — *`payout_v2` built under D9; next policy version D18* | One platform-standard hourly rate with admin-only campaign override (§16.1). | `payout_v3` resolves disclosed base and premium hourly amounts on an immutable rule revision; later rule changes never reprice accepted work. |
| Q5 | Payable-hour definition | **CONFIRMED** — *supersedes D9(c)/D12 zone-pricing statement via D18; parked-time detail fixed by D22* | Verified in-window, valid-GPS/movement campaign time earns a base hourly amount outside the primary/premium zone and a premium hourly amount inside it; exclusion zones, D22-classified stationary time after the shared grace, and invalid time are unpaid; D4 cap still applies (§16.1). | `payout_v3` freezes the accepted tier, complete eligibility terms and `stationary-rd-v1` marker; payout-v1/v2 remain immutable. The fingerprint, calculation/correction evidence and driver/admin explanations show the applied tier/reason. Later parked-time tuning requires a new effective revision for future acceptances. |
| Q6 | Campaign creation & approval | **CONFIRMED** | Advertiser creates a draft and submits it for admin approval before launch (§18). | Use one audited lifecycle; no parallel approval flag. |
| Q7 | Matching model | **CONFIRMED** | System recommends eligible drivers/vehicles; admin approves the final assignment (§21). | Recommendation and assignment remain separate steps. |
| Q8 | Driver acceptance | **CONFIRMED** | Driver receives terms-complete offer and accepts/declines before final assignment (§21). | Record offer, acceptance/decline, expiry and immutable accepted terms. |
| Q9 | Mid-flight changes | **CONFIRMED** | Safe expansions apply immediately inside funded headroom; reductions/removals/date changes require admin approval and reason (§18/§15.5). | Data-driven change classification; RM12 funding authority still wins. |
| Q10 | Session start/end | **CONFIRMED** (D18 supersedes D11 timing) | Pilot uses the screen-on installable driver PWA with driver-controlled Start/End and schedule/area checks; native background tracking follows after the pilot (§8.6/§23). | Preserve the identical backend contract so the later native client replaces only the client runtime, not campaign/payment semantics. |
| Q11 | Retargeting MVP shape | **CONFIRMED** (D18, clarified by D20; scope still bounded by D11) | Combine governed anonymised exposure segments/controlled export with optional direct Meta/Google geographic, time and contextual activation. Route/exposure data is never a person-level audience. Live push remains disabled until accounts, legal approval, API access and activation budget are present (§22). | Build one privacy-safe segment/source model and downstream adapter seam. Activation payloads allow only approved aggregate geography/time/context and reject identifiers/person-level records. `EXT-AD-PLATFORM` gates live contextual push; person-level exposure retargeting is outside the pilot and requires suitable lawful identity data plus a later approved partner. |
| Q12 | Advertiser results | **CONFIRMED** | Verified operational results plus clearly labelled estimated advertising results and downloadable report (§27). | Measured and modelled results remain structurally and visually distinct. |
| Q13 | Driver joining | **CONFIRMED** | Public self-registration with document upload and admin approval before work (§23). | D1 continues to govern advertisers/orgs only. |
| Q14 | Quotes/invoices in-platform | **CONFIRMED** | Platform generates numbered VAT-consistent invoices; quotations may be prepared externally, then the system records the agreed quotation/deal and accepted terms (§15). | External quote provenance and immutable accepted terms feed the invoice engine. Real issuance still waits for missing Q28 statutory facts. |

### B. MVP rules (Q15–Q24)

| Q | Topic | Status | Current direction | Build implication & divergence guard |
|---|-------|--------|-------------------|--------------------------------------|
| Q15 | Activation gates | **CONFIRMED** | Funding authority, approved creative, assigned eligible vehicles and approved installation evidence are required; admin performs final activation (§18). | Atomic gate checklist under RM12/RM13. |
| Q16 | One campaign per vehicle | **CONFIRMED** | One active campaign per vehicle. | Enforce the existing exclusivity invariant; remove the client-facing promise of later overlapping placements. |
| Q17 | Installation evidence | **CONFIRMED** | Admin-approved installation photo before campaign hours can earn (§19/§18). | Evidence state feeds activation and earnings eligibility. |
| Q18 | Creative upload & approval | **CONFIRMED** (confirms D7) | Advertiser uploads in-platform; admin approves/rejects before production or launch (§19/§18). | Build the managed file pipeline and review queue. |
| Q19 | Vehicle eligibility | **CONFIRMED** | Roadworthy cars meeting approved documentation/condition checks for the pilot. | Pilot validation accepts cars; other vehicle types are out of the confirmed pilot. |
| Q20 | Minimum activity | **CONFIRMED** | Configurable verified-hours floor and automatic flag after seven consecutive inactive days (§21). | Exact weekly minimum remains a pre-pilot operational parameter. |
| Q21 | Fraud handling | **CONFIRMED** (confirms D5) | Hold flagged-session earnings for admin review; show reasons and dispute path; thresholds configurable (§17). | Implement RM8 assessment/transition/hold contracts. |
| Q22 | Earnings release | **CONFIRMED** (D18) | Clean/current-successful assessed earnings become available for the next weekly batch. Flagged/under-review earnings remain pending for a seven-day review SLA; admin explicitly approves/releases or confirms/declines them. Unresolved cases remain held and escalate after day seven—never auto-release. | MNY-03A implements clean release plus flagged-review escalation. Named adjuster, separate approver and value-complete audit remain mandatory for corrections. |
| Q23 | Owner-drivers only | **CONFIRMED** | Pilot vehicle is registered to its driver and that driver is the payee; fleet owners are not onboarded during the pilot. | Keep the payee abstraction promised in the approved rule; it prevents later fleet support from rewriting money history. |
| Q24 | Cancellation & refunds | **CONFIRMED** (D18, clarified by D20) | The 24-hour window starts at the first confirmed cash allocation authorising production. Standard production waits until the window closes. An advertiser may request expedited production only by an immutable audited waiver; refund eligibility ends when waived production actually begins. Cancellation stops new work at an immutable cutoff and pays verified driver earnings to that moment. | Production authority must prove window expiry or a valid advertiser-requested waiver. Record waiver, actual production start, eligibility decision and settlement. Corporate-credit cancellations with no received cash have contract settlement, not a refund. |

### C. Pilot & launch (Q25–Q34)

| Q | Topic | Status | Current direction | Build implication & divergence guard |
|---|-------|--------|-------------------|--------------------------------------|
| Q25 | Production/permits ownership | **CONFIRMED** | Terrax Media coordinates platform-managed printing, installation, removal and permits through approved vendors; costs are itemised in the quotation. | Ops ownership plus itemisable commercial lines; actual Abuja permit evidence remains a launch gate. |
| Q26 | Driver onboarding requirements | **CONFIRMED checklist** / **OPEN artifacts** | Licence, vehicle registration, insurance, NIN, vehicle photos, renewal/recheck rules, and a legal/compliance-approved driver agreement/privacy consent are required. Verified bank account capture is also required by Q27. | Build the complete versioned checklist. Exact renewal periods and approved agreement/consent wording remain `EXT-LEGAL-PRIVACY` inputs. |
| Q27 | Payout channel | **CONFIRMED** (D18) | Automated bank transfers through an approved provider during the pilot (§16.3). | Provider adapter, signed submission/receipts, idempotency and per-line reconciliation are MVP requirements. `EXT-DISBURSEMENT-PROVIDER` gates live execution, not provider-neutral build/tests. |
| Q28 | VAT & invoice presentation | **CONFIRMED presentation** / **OPEN statutory facts** | Customer-facing prices and totals are VAT-inclusive, while the invoice still itemises the included net, VAT amount and gross. Business name is Terrax Media. | Keep net/VAT/gross immutable on issued invoices and default customer display to VAT-inclusive. TIN, billing address, invoice wording/numbering and accountant confirmation remain `EXT-Q28-COMPANY`. |
| Q29 | Product name & brand | **CONFIRMED** | Business: **Terrax Media**. Product/app: **Cardvert**. | The repository rename sweep is complete; domain, logo/assets and account action remain `EXT-RELEASE-ENV`. |
| Q30 | Pilot shape | **CONFIRMED shape and measures** / **OPEN permit evidence** (D20 reporting clarification) | Abuja; 10 vehicles; 5 paying advertisers; 3 months. Standard reporting is Campaign Performance Analysis: verified operations, clearly labelled modelled exposure and target-area coverage. Financial ROI is optional and appears only with defined advertiser conversion/revenue inputs and an approved reproducible methodology. | Omit financial ROI when either inputs or methodology are absent. The measurement contract defines the 60% numerator/denominator, input provenance and optional ROI method before acceptance. D19 assigns permit ownership; actual Abuja permit evidence remains `EXT-PILOT-PERMITS`. |
| Q31 | Privacy/consent/retention sign-off | **CONFIRMED owner type** / **OPEN artifacts** | Terrax Media's business legal/compliance adviser owns approval; retention remains configurable. | Named adviser, approved policy/consent wording, retention/DSR decisions and RM15 artifacts remain `EXT-LEGAL-PRIVACY`. Live GPS, advertiser outputs and activation remain gated. |
| Q32 | Infra ownership | **CONFIRMED ownership** / **OPEN action** | Client-owned cloud account and domain, with developer access for setup/maintenance. | Actual account/domain access, selected provider and recurring budget remain launch/staging inputs. |
| Q33 | Day-to-day operations owner | **CONFIRMED** | Developer supports initial pilot operations while training Somto's operations person/team in parallel. | Training/handover must name the receiving operator before go-live; the operating model itself is confirmed. |
| Q34 | Notification channels | **CONFIRMED** (D18, preference clarified by D24) | In-app plus default-on automated transactional email for advertisers, with one audited advertiser-organization opt-out; operations-run WhatsApp for drivers; automated SMS/WhatsApp after the pilot (§20). | Shared outbox + email adapter in MVP; in-app cannot be disabled; W2-04B consults the organization preference; driver WhatsApp is an audited manual task. |

### Cross-cutting dependency notes

- **Q14 → Q28**: invoice engine and external-quotation capture build now;
  issuing real invoices waits on the remaining statutory company facts.
- **Q11 → Q31**: segment aggregation builds now; export and aggregate
  geographic/time/context activation remain disabled until legal sign-off.
  Direct push also requires `EXT-AD-PLATFORM`; person-level exposure
  retargeting is outside the pilot per D20.
- **Q23/Q27**: owner-driver-only does not remove the payee abstraction; live
  automated transfer additionally requires `EXT-DISBURSEMENT-PROVIDER`.
- **Q13 vs D1**: D1 (operator-led onboarding) now applies to advertisers/orgs
  only; drivers self-register per Q13. Cite both when touching onboarding.
- **Q10 vs D11**: D18 is later direct client authority. Pilot delivery is the
  screen-on installable PWA; the native background-tracking app is post-pilot.

## Open questions

Per-question status lives in **Part 2 above** (D18–D20). Somto answered all 34
directions. Remaining OPEN items are execution facts/artifacts rather than
unanswered product choices: Q26/Q31 legal/privacy artifacts and renewal
periods, Q28 statutory invoice facts, Q29 brand/domain assets, Q30 Abuja permit
evidence and metric definition, and Q32 account/domain/budget action.

## How to use this log

- When an answer arrives, add a row with source "Somto, {date}".
- When a decision changes, add a new row that supersedes it — never edit
  history.
- Every build phase that implements a decision references its D-number in
  the commit message.
